{
  "name": "opapp-api",
  "version": "1.1.0",
  "private": true,
  "description": "OPAPP API – Cloudflare Worker + D1, trackt Allzeithochs & Kursverlauf der OPShards-Wechselkurse sowie den Spieler-Rekord des Servers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute opapp-shards --remote --file=./migrations/0001_init.sql",
    "db:seed": "wrangler d1 execute opapp-shards --remote --file=./migrations/0002_seed_all_time_high.sql",
    "db:migrate-peak": "wrangler d1 execute opapp-shards --remote --file=./migrations/0003_player_peak.sql",
    "db:ath": "wrangler d1 execute opapp-shards --remote --command=\"SELECT * FROM all_time_high;\"",
    "db:peak": "wrangler d1 execute opapp-shards --remote --command=\"SELECT * FROM player_count_peak;\""
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
